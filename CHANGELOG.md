# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.4.0] - 2026-09-23

品牌更新：站点更名为 KOKA-PhotoKit，全站启用新版透明底品牌图标。

### 变更
- 站点名称由「泡泡图片工坊」统一改为 **KOKA-PhotoKit**：页面标题、`meta description`、头部主标题、ZIP 打包文件名、水印默认文案、README
- 网站图标更换为新版透明底 SVG 吉祥物（`public/logo.svg`），同时用于 `rel="icon"` 与 `apple-touch-icon`
- 主标题左侧装饰性 CSS 泡泡替换为同一枚品牌图标，移除原渐变圆形底、描边与内阴影，改为 `object-fit: contain` 等比展示 + 柔和投影
- 站点语言标记由 `lang="zh-CN"` 保持不变，品牌名走英文

### 质量保障
- 图标渲染实测：透明背景、无底色矩形，猫形吉祥物轮廓完整
- 全仓检索确认无残留旧品牌名引用

## [1.3.0] - 2026-09-23

新增处理结果大图预览，支持原图/处理后对比与原始像素查看。

### 新增
- 大图预览弹窗：基于浏览器原生 `<dialog>` 元素实现（零新增依赖），自动焦点管理、支持 ESC 与点击遮罩关闭
- 三态对比：处理后 / 原图 / 左右对比（中缝分割线 + 原图·处理后角标）
- 缩放查看：适应窗口 / 100% 原始像素，100% 时容器可滚动，便于检查压缩画质
- 入口：任务项缩略图可点击放大，完成项新增 🔍 预览按钮；弹窗内可直接下载
- 宽高比发生变化时（如非等比改尺寸）自动隐藏「左右对比」，避免对比错位

### 变更
- 缩略图由 `<img>` 改为按钮包裹，带悬停放大反馈，明确可点击语义
- 无预览能力的格式（如 HEIC 原图）降级为提示 + 直接下载

### 质量保障
- Playwright 端到端验收：上传 → 处理 → 预览 → 三态切换 → 100% 缩放（1200px 实测）→ ESC/按钮关闭 → 缩略图重开，全链路通过，控制台零错误
- 28 个单元测试全绿，oxlint 0 告警

## [1.2.0] - 2026-09-22

前端重构：功能导航分组、头部版本号展示、批量操作栏按钮层级优化。

### 变更
- 功能菜单按用途分组导航：压缩（图片压缩 / 指定大小 / GIF 压缩）、尺寸（修改尺寸 / 修改 DPI / 图片裁剪）、格式（格式转换 / Base64）、美化（图片水印 / 图片滤镜）、AI（AI 抠图）
- 标题行新增版本号徽标（v1.2.0），构建期由 vite.config.ts 从 package.json 注入，单一数据源
- 批量操作栏按钮重组：主操作「开始处理」、次操作「打包下载 ZIP」、轻量「清空」，层级更清晰
- 副标题文案同步覆盖全部 11 项能力；移动端分组菜单与按钮栏适配优化

## [1.1.0] - 2026-09-22

新增 5 个工具（对标 tools.jvxiao.cn 能力补齐）。

### 新增
- 图片裁剪：自由 / 1:1 / 4:3 / 3:4 / 16:9 / 9:16 预设比例，宽高与位置百分比调节，越界自动收敛
- 图片水印：文字内容 / 字号 / 透明度 / 五宫格位置 / 颜色 / 边距，PNG 输出
- 图片滤镜：亮度 / 对比度 / 饱和度 / 模糊半径，100% 为原图效果
- Base64 互转：图片 → Base64（复制 / 下载 .txt），Base64 → 图片解码预览，独立工具不占批量管线
- AI 抠图 / 背景替换：@imgly/background-removal（AGPL-3.0）+ ONNX Runtime Web 本地推理，支持纯色与渐变背景合成，图片不上传

### 变更
- ModeId 扩展 6 个新模式；pipeline 支持裁剪 → 水印 → 滤镜链式变换
- AI 抠图依赖 DOM（onnxruntime 资源加载），主线程执行；其余新模式沿用 Web Worker 并行

### 质量保障
- 新增裁剪、Base64 单元测试；28 个测试全绿
- 浏览器端到端验收：裁剪 / 水印 / 滤镜真实图片处理通过，无控制台报错

## [1.0.0] - 2026-09-22

首次发布。

### 新增
- 图片压缩：固定质量滑杆（MozJPEG / OxiPNG / libwebp / AVIF）
- 指定大小压缩：质量二分搜索 + 尺寸阶梯兜底，支持 KB/MB 目标
- 格式转换：JPG / PNG / WebP / AVIF 互转，透明像素自动填白底
- 修改尺寸：像素 / 百分比两种模式，支持锁定纵横比
- 修改 DPI：纯字节补丁不重编码，JPEG（JFIF + EXIF 双写）与 PNG（pHYs 块）均支持
- GIF 压缩：gifsicle lossy 二分 + 尺寸阶梯，支持指定目标大小
- HEIC/HEIF 导入（自动转 JPEG）、BMP 回退解码
- 批量处理：最多 100 张，Web Worker 并行，单张下载与 ZIP 打包
- Frutiger Aero 界面：静态三段渐变背景 + 玻璃拟态卡片（无循环动画，低渲染开销）
- 纯本地处理：全部基于 WebAssembly 在浏览器内完成，图片不上传

### 质量保障
- 19 个单元测试（压缩二分、DPI 补丁、格式推断），Vitest 全绿
